# Discuss PRD — {project_name}
<!-- template_path: aah/_resources/_templates/discuss/discuss-prd.md -->
<!-- Written by /aah-discuss Step 10b to .aah/discuss/discuss-prd.md.
     Read the slug registry at .aah/discuss/decision-registry.yaml and
     the confirmed header at .aah/discuss/prd-header.md. Fill placeholders
     with real content — do NOT leave `{placeholder}` markers or HTML comments
     in the final file.

     Prose sections (Problem Statement / Solution / User Stories) come from
     the confirmed prd-header.md verbatim (the user already approved them at
     Step 10). Do NOT rewrite them here.
       - Problem Statement: quotes the user's actual brief; states who, what,
         why now. No invented motivation.
       - Solution: comprehensive prose synthesis of EVERY decision in the
         registry (both pre_resolved[] and decisions[]). Every claim grounded
         in a specific slug_id. A reader who never watched the walk should
         get the whole picture here.
       - User Stories: derived from end-users + solution-flow decisions.

     Data sections (Overview, Decisions, Pre-Resolved, Constraints, Constraint
     Audit) project what's in the registry — no invented facts. The PRD carries no
     port/activation section (that misled readers into running ports as next steps). -->

<!-- === Confirmed header sections — copy body verbatim from prd-header.md ===
     The user already approved these three sections at Step 10. Do NOT rewrite. -->

## Problem Statement

{problem_statement}

## Solution

{solution}

## User Stories

{user_stories}

## Overview & Intent

| Field | Value |
|-------|-------|
| Project | `{project_name}` |
| Complexity tier | `{complexity}` |
| Business domain(s) | {domains} |
| Technical archetype(s) | {archetypes} |
| Discovery started | {started_at} |

## Scope

**In scope** — every decision recorded below.

<!-- If registry.deferred_ideas is non-empty, render each as a bullet under
     "Deferred:". Otherwise omit the Deferred subsection entirely. -->
{scope_deferred_block}

## Decisions Made

<!-- One row per entry in registry.decisions[]. Sort in walk order.
     Mark emergent decisions with a trailing ⚡ on the slug_id.
     Rationale = decision.notes (or "Emergent" if emergent and no notes). -->

| slug_id | Area | Decision | Rationale |
|---------|------|----------|-----------|
{decisions_rows}

<!-- If ANY decision has revised_from[], add this subsection. Otherwise omit. -->
{revisions_block}

## Pre-Resolved (brownfield / inferred)

<!-- One row per entry in registry.pre_resolved[].
     Source column = the entry's source (user | inferred | codebase).
     If pre_resolved is empty, replace this table with the italic line "_None._" -->

| slug_id | Value | Source | Inferred? |
|---------|-------|--------|-----------|
{preresolved_rows}

## Constraints & Compliance

<!-- Summarize the constraint_audit block from the registry:
     - passed == null   → "_Constraint gate has not run yet._"
     - passed == true   → "Constraint gate: **PASS** (no blocking violations)."
     - passed == false  → "Constraint gate: **FAIL** — see Constraint Audit below."
     If audit.flags is non-empty, list under "**Flags (non-blocking):**".
     If audit.warnings is non-empty, list under "**Warnings:**". -->
{constraints_summary}

## Open Questions for Analysis

_None — resolved during discovery._

## Constraint Audit — Checks A-F

<!-- If audit.passed is null, replace the table below with "_Not run._"
     Otherwise render:
     - **Overall:** ✅ PASS or ⚠️ FAIL
     - **Ran at:** {audit.ran_at}
     - One row per check in audit.checks (name | result uppercased | detail)
     - If audit.violations is non-empty, list them under "**Blocking violations:**" -->

**Overall:** {audit_overall}
**Ran at:** {audit_ran_at}

| Check | Result | Detail |
|-------|--------|--------|
{audit_check_rows}

{audit_violations_block}

---

_Generated by /aah-discuss Step 10b from the slug registry + confirmed header.
Do not hand-edit — regenerate by re-running Step 10b._
